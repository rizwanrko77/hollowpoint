{
  "name": "hollowpoint-apply",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2024-11-01",
    "deploy": "wrangler pages deploy public --project-name=hollowpoint-apply"
  },
  "devDependencies": { "wrangler": "^3.90.0" }
}
